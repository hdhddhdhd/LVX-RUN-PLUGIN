# pytouch-run — 官方插件调用 / Official Plugin Call

## 中文

**包信息**：pytouch-run 1.0.0，runtime id `pytorch-run`。
**状态**：占位实现（capabilities 0x0，开发中）。probe 不认领真实模型；直接调用
预期失败——这是设计行为，不是 bug。
**调用**：暂无可用路径。跟踪上游发布即可。


## English

**Package**: pytouch-run 1.0.0, runtime id `pytorch-run`.
**Status**: placeholder (capabilities 0x0, WIP). It does not claim real models;
direct invocation fails on purpose — that is expected behaviour.
**Invocation**: none yet; watch upstream releases.

